@model List<PersonelSonucDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Yonetici/Index.cshtml";
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Personel Listesi</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Personel Adı</th>
                                <th scope="col">Unvan</th>
                                <th scope="col">Telefon</th>
                                <th scope="col">Durum</th>

                                <th scope="col">Sil</th>
                                <th scope="col">Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <th scope="row">@count</th>

                                    <td>@item.PersonelIsim</td>
                                    <td>@item.Baslik</td>
                                    <td>@item.Telefon</td>
                                    
                                    <td>
                                        @if (item.Durum)
                                        {
                                            <span>Aktif</span>
                                        }
                                        else
                                        {
                                            <span>Pasif</span>
                                        }
                                    </td>
                                    <td><a href="/Personel/PersonelSil/@item.PersonelID" class=" btn btn-outline-danger"> Sil </a></td>
                                    <td><a href="/Personel/PersonelGuncelle/@item.PersonelID" class=" btn btn-outline-success"> Güncelle </a></td>
                                </tr>
                            }

                        </tbody>
                    </table>
                    <a href="/Personel/PersonelOlustur/" class=" btn btn-outline-info"> Yeni Personel Girişi </a>
                </div>
            </div>

        </div>
    </div>
</div>

